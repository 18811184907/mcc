# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Env / secrets
.env
.env.*
!.env.example

# 测试生成的备份
**/*.backup-*
**/settings.json.backup-*
**/config.toml.backup-*

# 临时文件
*.tmp
*.temp
.claude/PRPs/**/state.json
.full-stack-feature/
.full-review/

# 不要 ignore 的（显式保留）
!.gitkeep
!manifest.json
!plugin.json
!marketplace.json

# dist/ 要 commit（/plugin install 会用）
# source/ 要 commit（真相）
# adapters/ 要 commit
# scripts/ 要 commit
# _decisions/ 要 commit（决策记录给贡献者看）

# MCC PROJECT_VAULT (auto-managed — secrets must never enter git)
.claude/PROJECT_VAULT.md
.env.local
.env.*.local
.deploy.local.md
